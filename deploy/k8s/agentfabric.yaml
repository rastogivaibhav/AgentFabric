# ─── Namespace ────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Namespace
metadata:
  name: agentfabric
  labels:
    app.kubernetes.io/name: agentfabric

---
# ─── ConfigMap ────────────────────────────────────────────────────────────────
apiVersion: v1
kind: ConfigMap
metadata:
  name: agentfabric-config
  namespace: agentfabric
data:
  collector.yaml: |
    debug: false
    grpc:
      addr: ":4317"
    http:
      addr: ":4318"
    auth:
      require_auth: true
    rate_limit:
      spans_per_second: 100000
      burst_size: 20000
    processor:
      batch_size: 512
      batch_timeout_ms: 200
      max_queue_size: 500000
      workers: 8
      max_attribute_len: 4096
    pii:
      enabled: true
      redact: true
    discovery:
      enabled: true
      kubernetes_mode: true
      process_scan_seconds: 60

---
# ─── Secret ───────────────────────────────────────────────────────────────────
# Replace every REPLACE_* value before applying to a cluster.
# Generate secrets: openssl rand -base64 32
apiVersion: v1
kind: Secret
metadata:
  name: agentfabric-secrets
  namespace: agentfabric
type: Opaque
stringData:
  jwt-secret:       "REPLACE_WITH_STRONG_SECRET_MIN_32_CHARS"
  jwt-secrets:      ""   # comma-separated for zero-downtime rotation (optional)
  admin-password:   "REPLACE_WITH_STRONG_ADMIN_PASSWORD"
  gateway-auth-token: "REPLACE_WITH_SHARED_INGEST_BEARER_TOKEN"
  vault-key:        "REPLACE_WITH_64_CHAR_HEX_KEY"
  oidc-client-secret: ""  # set when AF_SSO_REQUIRED=true
  database-url:     "postgres://fabric:REPLACE_PASSWORD@postgres-svc:5432/agentfabric?sslmode=require"
  redis-url:        "redis://:REPLACE_PASSWORD@redis-svc:6379"

---
# ─── Collector DaemonSet ──────────────────────────────────────────────────────
# Runs one pod per node for local-path OTLP ingestion (no network hop).
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: agentfabric-collector
  namespace: agentfabric
  labels:
    app: agentfabric-collector
spec:
  selector:
    matchLabels:
      app: agentfabric-collector
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: agentfabric-collector
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      serviceAccountName: agentfabric-collector
      terminationGracePeriodSeconds: 30
      containers:
        - name: collector
          image: ghcr.io/agentfabric/collector:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4317  # gRPC OTLP
              protocol: TCP
            - containerPort: 4318  # HTTP OTLP
              protocol: TCP
            - containerPort: 9090  # Prometheus
              protocol: TCP
          env:
            - name: AF_ENV
              value: "production"
            - name: AF_STRICT_CONFIG
              value: "true"
            - name: AF_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: AF_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: jwt-secret
            - name: AF_GATEWAY_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: gateway-auth-token
            - name: AF_GATEWAY_ENDPOINT
              value: "http://agentfabric-gateway-svc:8080"
          volumeMounts:
            - name: config
              mountPath: /etc/agentfabric
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 4318
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 4318
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: config
          configMap:
            name: agentfabric-config

---
# ─── Collector Service ────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: agentfabric-collector-svc
  namespace: agentfabric
spec:
  selector:
    app: agentfabric-collector
  type: ClusterIP
  clusterIP: None  # headless — SDKs hit the local node pod directly
  ports:
    - name: grpc-otlp
      port: 4317
      targetPort: 4317
    - name: http-otlp
      port: 4318
      targetPort: 4318

---
# ─── API Gateway Deployment ───────────────────────────────────────────────────
# The REST control plane can scale horizontally, but /api/v1/stream/live uses
# a process-local WebSocket hub and is not a supported multi-replica HA path.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentfabric-gateway
  namespace: agentfabric
spec:
  replicas: 3
  selector:
    matchLabels:
      app: agentfabric-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0        # zero-downtime: new pod must be ready before old is removed
  template:
    metadata:
      labels:
        app: agentfabric-gateway
    spec:
      terminationGracePeriodSeconds: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: agentfabric-gateway
                topologyKey: kubernetes.io/hostname
      containers:
        - name: gateway
          image: ghcr.io/agentfabric/api-gateway:1.0.0
          ports:
            - containerPort: 8080
          env:
            - name: AF_ENV
              value: "production"
            - name: AF_STRICT_CONFIG
              value: "true"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: redis-url
            - name: AF_CORS_ORIGINS
              value: "https://app.agentfabric.io"
            - name: AF_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: jwt-secret
            - name: AF_GATEWAY_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: gateway-auth-token
            # AF_JWT_SECRETS: set for zero-downtime key rotation.
            # When empty the gateway falls back to AF_JWT_SECRET.
            - name: AF_JWT_SECRETS
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: jwt-secrets
            - name: AF_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: admin-password
            - name: AF_VAULT_KEY
              valueFrom:
                secretKeyRef:
                  name: agentfabric-secrets
                  key: vault-key
            # Uncomment and populate these when enabling enterprise SSO.
            # AF_SSO_REQUIRED=true requires issuer, client ID, client secret, and redirect URI together.
            # - name: AF_SSO_REQUIRED
            #   value: "true"
            # - name: AF_PASSWORD_LOGIN_DISABLED
            #   value: "true"
            # - name: AF_OIDC_ISSUER
            #   value: "https://login.example.com/tenant/v2.0"
            # - name: AF_OIDC_CLIENT_ID
            #   value: "agentfabric-portal"
            # - name: AF_OIDC_CLIENT_SECRET
            #   valueFrom:
            #     secretKeyRef:
            #       name: agentfabric-secrets
            #       key: oidc-client-secret
            # - name: AF_OIDC_REDIRECT_URI
            #   value: "https://app.agentfabric.io/auth/callback"
            # - name: AF_OIDC_LOGOUT_URL
            #   value: "https://login.example.com/logout"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true

---
apiVersion: v1
kind: Service
metadata:
  name: agentfabric-gateway-svc
  namespace: agentfabric
spec:
  selector:
    app: agentfabric-gateway
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080

# ─── Portal Deployment ────────────────────────────────────────────────────────
# Nginx serving the pre-built React bundle.  Stateless — scale freely.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentfabric-portal
  namespace: agentfabric
  labels:
    app: agentfabric-portal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agentfabric-portal
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: agentfabric-portal
    spec:
      terminationGracePeriodSeconds: 15
      containers:
        - name: portal
          image: ghcr.io/agentfabric/portal:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          securityContext:
            runAsNonRoot: false  # nginx worker needs port 80
            allowPrivilegeEscalation: false

---
apiVersion: v1
kind: Service
metadata:
  name: agentfabric-portal-svc
  namespace: agentfabric
spec:
  selector:
    app: agentfabric-portal
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80

---
# ─── RBAC for collector (K8s API discovery) ───────────────────────────────────
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentfabric-collector
  namespace: agentfabric

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: agentfabric-collector
rules:
  - apiGroups: [""]
    resources: ["pods", "nodes", "namespaces"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "daemonsets"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: agentfabric-collector
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: agentfabric-collector
subjects:
  - kind: ServiceAccount
    name: agentfabric-collector
    namespace: agentfabric

---
# ─── HorizontalPodAutoscaler ──────────────────────────────────────────────────
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agentfabric-gateway-hpa
  namespace: agentfabric
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agentfabric-gateway
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

# ─── PodDisruptionBudgets ─────────────────────────────────────────────────────
# Ensure at least one pod of each critical service survives voluntary disruptions
# (node drains, rolling cluster upgrades).  Without PDBs, a multi-node drain
# can take all pods offline simultaneously.
#
# api-gateway: minAvailable:1 with 3 replicas → two pods can drain at once.
# This improves REST/API availability, but does not make /api/v1/stream/live
# replica-safe because live fan-out remains in-memory inside each pod.
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: agentfabric-gateway-pdb
  namespace: agentfabric
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: agentfabric-gateway

---
# collector (DaemonSet): minAvailable:1 prevents simultaneous drain of ALL nodes.
# This is most relevant in single-AZ clusters; multi-AZ clusters are inherently
# protected by zone topology, but the PDB is cheap insurance.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: agentfabric-collector-pdb
  namespace: agentfabric
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: agentfabric-collector
