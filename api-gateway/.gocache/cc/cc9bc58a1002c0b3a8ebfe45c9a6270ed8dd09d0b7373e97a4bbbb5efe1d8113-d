//line C:\Users\vrast\Documents\Agentic Code\files\api-gateway\internal\budget\budget.go:1:1
package budget

import (
	"context"
	"sync"
	"time"

	"go.uber.org/zap"
)

// Budget represents a tenant's monthly quota
type Budget struct {
	TenantID       string
	MonthlyTokens  int64
	MonthlyCostUSD float64
	AlertThreshold float64 // 0.80 = alert at 80%
	HardLimit      bool    // true = block at 100%, false = alert only
	ResetDay       int     // day of month to reset
	CreatedAt      time.Time
	UpdatedAt      time.Time
}

// UsageSummary represents current period usage
type UsageSummary struct {
	TokensUsed  int64
	CostUsedUSD float64
	PeriodStart time.Time
	PeriodEnd   time.Time
}

// Store interface for budget queries
type Store interface {
	GetBudget(ctx context.Context, tenantID string) (*Budget, error)
	GetMonthlyUsage(ctx context.Context, tenantID string, since time.Time) (*UsageSummary, error)
	UpsertBudget(ctx context.Context, b *Budget) error
	RecordAlert(ctx context.Context, tenantID, alertType string, usage UsageSummary, b Budget) error
	ListBudgetAlerts(ctx context.Context, tenantID string, limit int) ([]map[string]interface{}, error)
}

// Alerter sends notifications when budgets are hit
type Alerter interface {
	Fire(ctx context.Context, tenantID, alertType string, usage UsageSummary, budget Budget) error
}

// BudgetEnforcer checks and records token/cost usage against budgets
type BudgetEnforcer struct {
	store      Store
	alerter    Alerter
	logger     *zap.Logger
	mu         sync.RWMutex
	alertedAt  map[string]time.Time // dedup: tenant+alertType -> last alert time
}

// NewBudgetEnforcer creates a new enforcer
func NewBudgetEnforcer(store Store, alerter Alerter, logger *zap.Logger) *BudgetEnforcer {goCover_e230295c5dde__0[0] = 1 ; goCover_e230295c5dde__0[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__0[2] = 0 ; goCover_e230295c5dde__0[3]++;
	return &BudgetEnforcer{
		store:     store,
		alerter:   alerter,
		logger:    logger,
		alertedAt: make(map[string]time.Time),
	}
}

// CheckAndRecord checks usage against budget and records the usage
// Returns (allowed bool, err error)
// If allowed=false, the ingest should return HTTP 429
// If err != nil, we failed to check but don't block (fail open)
func (e *BudgetEnforcer) CheckAndRecord(
	ctx context.Context,
	tenantID string,
	incomingTokens int64,
	incomingCostUSD float64,
) (bool, error) {goCover_e230295c5dde__1[0] = 19 ; goCover_e230295c5dde__1[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__1[2] = 1 ; goCover_e230295c5dde__1[3]++;
	if tenantID == "" {goCover_e230295c5dde__1[10]++;
		return true, nil // no budget for empty tenant
	}

	goCover_e230295c5dde__1[4]++;budget, err := e.store.GetBudget(ctx, tenantID)
	if err != nil {goCover_e230295c5dde__1[11]++;
		// Budget lookup failed — fail open (don't block), log error
		e.logger.Warn("failed to get budget", zap.String("tenant", tenantID), zap.Error(err))
		return true, nil
	}

	// No budget set = unlimited
	goCover_e230295c5dde__1[5]++;if budget == nil || (budget.MonthlyTokens == 0 && budget.MonthlyCostUSD == 0) {goCover_e230295c5dde__1[12]++;
		return true, nil
	}

	// Get current period start based on reset_day
	goCover_e230295c5dde__1[6]++;periodStart := e.currentPeriodStart(budget.ResetDay)
	usage, err := e.store.GetMonthlyUsage(ctx, tenantID, periodStart)
	if err != nil {goCover_e230295c5dde__1[13]++;
		e.logger.Warn("failed to get usage", zap.String("tenant", tenantID), zap.Error(err))
		return true, nil
	}

	// Check token limit
	goCover_e230295c5dde__1[7]++;if budget.MonthlyTokens > 0 {goCover_e230295c5dde__1[14]++;
		projected := usage.TokensUsed + incomingTokens
		percentUsed := float64(projected) / float64(budget.MonthlyTokens)

		if projected > budget.MonthlyTokens && budget.HardLimit {goCover_e230295c5dde__1[16]++;
			// Hard limit hit — block and alert
			e.fireAlert(ctx, tenantID, "hard_limit", *usage, *budget)
			return false, nil
		}

		goCover_e230295c5dde__1[15]++;if percentUsed > budget.AlertThreshold {goCover_e230295c5dde__1[17]++;
			// Threshold hit — alert but allow
			e.fireAlert(ctx, tenantID, "threshold_80", *usage, *budget)
		}
	}

	// Check cost limit
	goCover_e230295c5dde__1[8]++;if budget.MonthlyCostUSD > 0 {goCover_e230295c5dde__1[18]++;
		projected := usage.CostUsedUSD + incomingCostUSD
		percentUsed := projected / budget.MonthlyCostUSD

		if projected > budget.MonthlyCostUSD && budget.HardLimit {goCover_e230295c5dde__1[20]++;
			// Hard limit hit — block and alert
			e.fireAlert(ctx, tenantID, "cost_hard_limit", *usage, *budget)
			return false, nil
		}

		goCover_e230295c5dde__1[19]++;if percentUsed > budget.AlertThreshold {goCover_e230295c5dde__1[21]++;
			// Threshold hit — alert but allow
			e.fireAlert(ctx, tenantID, "cost_threshold_80", *usage, *budget)
		}
	}

	goCover_e230295c5dde__1[9]++;return true, nil
}

// fireAlert fires an alert, with deduplication to avoid flooding
func (e *BudgetEnforcer) fireAlert(
	ctx context.Context,
	tenantID string,
	alertType string,
	usage UsageSummary,
	budget Budget,
) {goCover_e230295c5dde__2[0] = 8 ; goCover_e230295c5dde__2[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__2[2] = 2 ; goCover_e230295c5dde__2[3]++;
	key := tenantID + ":" + alertType
	now := time.Now()

	e.mu.Lock()
	lastAlert, exists := e.alertedAt[key]
	e.mu.Unlock()

	// Dedupe: only fire once per hour
	if exists && now.Sub(lastAlert) < time.Hour {goCover_e230295c5dde__2[5]++;
		return
	}

	goCover_e230295c5dde__2[4]++;go func() {goCover_e230295c5dde__2[6]++;
		if err := e.alerter.Fire(ctx, tenantID, alertType, usage, budget); err != nil {goCover_e230295c5dde__2[9]++;
			e.logger.Warn("alert fire failed", zap.String("tenant", tenantID), zap.String("type", alertType), zap.Error(err))
			return
		}

		// Record in DB
		goCover_e230295c5dde__2[7]++;if err := e.store.RecordAlert(ctx, tenantID, alertType, usage, budget); err != nil {goCover_e230295c5dde__2[10]++;
			e.logger.Warn("failed to record alert", zap.String("tenant", tenantID), zap.Error(err))
		}

		goCover_e230295c5dde__2[8]++;e.mu.Lock()
		e.alertedAt[key] = now
		e.mu.Unlock()
	}()
}

// currentPeriodStart returns the start of the current billing period
func (e *BudgetEnforcer) currentPeriodStart(resetDay int) time.Time {goCover_e230295c5dde__3[0] = 5 ; goCover_e230295c5dde__3[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__3[2] = 3 ; goCover_e230295c5dde__3[3]++;
	now := time.Now().UTC()
	year, month, day := now.Date()

	if day >= resetDay {goCover_e230295c5dde__3[6]++;
		// Current period started on resetDay of this month
		return time.Date(year, month, resetDay, 0, 0, 0, 0, time.UTC)
	}

	// Current period started on resetDay of previous month
	goCover_e230295c5dde__3[4]++;prevMonth := month - 1
	prevYear := year
	if prevMonth < 1 {goCover_e230295c5dde__3[7]++;
		prevMonth = 12
		prevYear--
	}
	goCover_e230295c5dde__3[5]++;return time.Date(prevYear, time.Month(prevMonth), resetDay, 0, 0, 0, 0, time.UTC)
}

// NoopAlerter is a no-op alerter for testing
type NoopAlerter struct{}

func (n *NoopAlerter) Fire(ctx context.Context, tenantID, alertType string, usage UsageSummary, budget Budget) error {goCover_e230295c5dde__4[0] = 1 ; goCover_e230295c5dde__4[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__4[2] = 4 ; goCover_e230295c5dde__4[3]++;
	return nil
}

// WebhookAlerter sends alerts via HTTP webhook (stub for now)
type WebhookAlerter struct {
	logger *zap.Logger
}

func NewWebhookAlerter(logger *zap.Logger) *WebhookAlerter {goCover_e230295c5dde__5[0] = 1 ; goCover_e230295c5dde__5[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__5[2] = 5 ; goCover_e230295c5dde__5[3]++;
	return &WebhookAlerter{logger: logger}
}

func (w *WebhookAlerter) Fire(ctx context.Context, tenantID, alertType string, usage UsageSummary, budget Budget) error {goCover_e230295c5dde__6[0] = 1 ; goCover_e230295c5dde__6[1] = goCover_e230295c5dde_P ; goCover_e230295c5dde__6[2] = 6 ; goCover_e230295c5dde__6[3]++;
	// TODO: Implement webhook dispatch
	w.logger.Debug("alert fired", zap.String("tenant", tenantID), zap.String("type", alertType))
	return nil
}
