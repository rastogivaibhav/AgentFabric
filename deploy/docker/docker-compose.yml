version: '3.9'

# Govagn — Minimal production stack (no Kafka/ClickHouse/monitoring)
# Run from the repo root: docker compose -f deploy/docker/docker-compose.yml up -d

services:
  # ─── Infrastructure ──────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: govagn
      POSTGRES_USER: fabric
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ../../deploy/sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fabric -d govagn"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Govagn Collector ────────────────────────────────────────────────────
  collector:
    build:
      context: ../../collector
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      GV_GRPC_ADDR: ":4317"
      GV_HTTP_ADDR: ":4318"
      GV_GATEWAY_ENDPOINT: "http://api-gateway:8080"
      GV_JWT_SECRET: ${GV_JWT_SECRET:-dev-secret-change-in-prod}
      GV_AUTH_REQUIRE_AUTH: "false"  # disable for local dev
      GV_GATEWAY_AUTH_TOKEN: ${GV_GATEWAY_AUTH_TOKEN:-dev-token-for-local-use}
      GV_PROCESSOR_WORKERS: "4"
      GV_NODE_NAME: "local-collector"
    ports:
      - "4317:4317"   # gRPC OTLP
      - "4318:4318"   # HTTP OTLP
      - "9090:9090"   # Prometheus metrics
    depends_on:
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4318/healthz"]
      interval: 15s
      timeout: 5s
      retries: 3

  # ─── API Gateway ─────────────────────────────────────────────────────────────
  api-gateway:
    build:
      context: ../../api-gateway
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: "postgres://fabric:${POSTGRES_PASSWORD:-changeme}@postgres:5432/govagn?sslmode=disable"
      REDIS_URL: "redis://redis:6379"
      GV_JWT_SECRET: ${GV_JWT_SECRET:-dev-secret-change-in-prod}
      LISTEN_ADDR: ":8080"
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Portal ───────────────────────────────────────────────────────────────────
  portal:
    build:
      context: ../../portal
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      VITE_API_URL: ${PORTAL_API_URL:-http://localhost:8080}
    ports:
      - "3000:80"
    depends_on:
      - api-gateway

volumes:
  pg_data:
  redis_data:

networks:
  default:
    name: govagn
